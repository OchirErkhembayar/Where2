<div class="container">
  <%= simple_form_for([current_user, @friendship], remote: true) do |f| %>
  <div class="form-group mb-3 mx-5 px-5">
      <%= f.input :user, collection: User.all, label_method: :first_name, input_html: {multiple: true, data: {controller: 'select2', select2_options_value: {tags: true}}}, placeholder: "Add your friends" %>
    <%= f.submit "Send friend request", class: "btn btn-primary mb-3 w-100" %>
  </div>
  <% end %>
</div>
