  <% if user_signed_in? %>
  <h1 class="dash-text">My Dashboard</h1>
   <div class="home-grid">
   <div class="grid-section"></div>
     <div class="grid-section"> <%= link_to "groups", groups_path, class: "btn btn-datway" %> </div>
     <div class="grid-section"></div>
     <div class="grid-section"> <%= link_to "events", event_users_path, class: "btn btn-datway" %> </div>
     <div style="background-image: url(<%= asset_path "home-image.png" %>)" class="home-image"></div>
     <div class="grid-section"> <%= link_to "#{@notification_count} invitation#{@notification_count == 1 ? "" : "s"}", usergroups_path(current_user), class: "btn btn-datway" %> </div>
     <div class="grid-section"></div>
     <div class="grid-section"> <%= link_to "past events", events_path, class: "btn btn-datway" %> </div>
     <div class="grid-section"></div>
   </div>
   <% else %>
    <div style="background-image: url(<%= asset_path "background.jpg" %>)" class="background-landing">
      <div class="frame">
        <p  class="landing-page-text">At Where2, we have created a new and fun way for you and your friends to connect and organise events. Are you tired of having not having one place where you can organise all your events with your friends, well now you can.  With Where2 you can now organise, plan and search for events in your local area. Planning has never been easier !</p>
        <%= link_to new_user_registration_path do %>
          <div class="button-landing-sign">
            <span style="color: white;">Sign up</span>
            <svg>
            <polyline class="o1" points="0 0, 150 0, 150 55, 0 55, 0 0"></polyline>
            <polyline class="o2" points="0 0, 150 0, 150 55, 0 55, 0 0"></polyline>
          </svg>
          </div>
        <% end %>
      </div>
    </div>
  <% end %>
